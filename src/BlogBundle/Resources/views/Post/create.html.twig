{% extends 'BlogBundle::layout.html.twig' %}

{% block content %}
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit"></input>
    {{ form_end(form) }}
{% endblock %}
